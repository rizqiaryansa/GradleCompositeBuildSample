package com.example.math

class Operation {
    companion object {
        fun add(a: Double, b: Double) = a + b

        fun substract(a: Double, b: Double) = a - b

        fun divide(a: Double, b: Double) = a / b

        fun multiply(a: Double, b: Double) = a * b
    }
}