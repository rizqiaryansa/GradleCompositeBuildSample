rootProject.name = 'calculator'

if (new File("composite-enable").exists() && new File("../mathutil").exists()) {
    includeBuild("../mathutil") {
        dependencySubstitution {
            substitute(module("com.sample.math:mathutil")).with(project(":"))
        }
    }
}